import { PrototypeConfig } from 'graphix-engine';
import { EdgeType } from './types';
import { CommonLineView } from './shape';

const prototype: PrototypeConfig = {
  type: EdgeType.Edge,
  view: CommonLineView,
  props: [],
  settings: [],
};

export default prototype;
